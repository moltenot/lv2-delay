#define DELAY_PORT_INDEX 0
#define INPUT_PORT_INDEX 1
#define OUTPUT_PORT_INDEX 2
#define WET_PORT_INDEX 3
#define BUFFER_SIZE 132300 // number of floats to keep in the buffer This is 44100 * 3 so we have 3 seconds of audio in the buffer